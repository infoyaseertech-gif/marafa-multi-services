/* =====================================================================
   MARAFA OPERATIONS PORTAL — Supabase connection config
   ---------------------------------------------------------------------
   Fill in the two values below from your Supabase project:
   Dashboard → Project Settings → API → "Project URL" and "anon public" key.
   This "anon" key is SAFE to put in front-end code — it only allows what
   your Row Level Security policies (from database/schema.sql) permit.
   NEVER put your "service_role" key here or anywhere in this website.
===================================================================== */
const SUPABASE_URL = "PASTE_YOUR_SUPABASE_PROJECT_URL_HERE";
const SUPABASE_ANON_KEY = "PASTE_YOUR_SUPABASE_ANON_KEY_HERE";
